import { ScrollView } from "react-native"
import { WishListContainer } from "./WishListContainer"

export default () => {
  return (
    <ScrollView>
      <WishListContainer />
    </ScrollView>
  )
}